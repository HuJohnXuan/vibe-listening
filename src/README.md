# 源码边界

当前已建立后续步骤所需的目录边界，并完成本地示例数据、推荐核心与播放器控制：

- `core/`：负责推荐排序，以及播放器索引循环和时间格式纯函数；
- `services/`：当前负责生成 3 首 Picks 与四路线 Radar 输出；
- `adapters/`：保存本地曲库、试听媒体路径和人工推荐关系；
- `ui/`：播放器页面、浏览器本地音频状态及同步后的当前歌曲视图；
- `types/`：跨层类型契约。
